{% extends "layout.njk" %}

{% block header %}
    <title>The Company - Courses</title>
{% endblock %}

{% block content %}
    <div id="wrapper">
        <h1>Principais cursos:</h1>
    </div>
    <section class="cards">

        {% for link in links %}
            <div class="card" id="{{ link.id }}">
                <div class="card__logo">
                    <img src="{{ link.img_url }}" alt="{{ link.alt_img }}"/>
                </div>
                <div class="card__modules">
                    <p>{{ link.modules }}
                        Módulos</p>
                </div>
                <div class="card__category">
                    <p>{{ link.category }}</p>
                </div>
            </div>

        {% endfor %}

    </section>
    <div class="modal-overlay">
        <div class="modal">
            <div class="close-modal">&times</div>
            <div class="modal-content">
                <iframe src="" frameborder="0"></iframe>
            </div>
        </div>
    </div>
    <script src="./scripts.js"></script>

    {% endblock%}